---
phase: 02.1-new-user-onboarding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - references/api-reference.md
autonomous: true
requirements:
  - ONBOARD-07

must_haves:
  truths:
    - "references/api-reference.md contains no occurrences of the maintainer-specific domain coolify.cicd.streamlinity.com"
    - "references/api-reference.md contains no occurrences of the maintainer-specific Doppler account name streamlinity"
    - "The file opens with a one-line note instructing the reader to substitute <your-coolify-domain> and <your-doppler-account> throughout"
    - "All Coolify URL examples use <your-coolify-domain> as the host placeholder; all doppler --account examples use <your-doppler-account>"
  artifacts:
    - path: "references/api-reference.md"
      provides: "Domain-neutral Coolify + Doppler API reference"
      contains: "<your-coolify-domain>"
  key_links:
    - from: "references/api-reference.md top of file"
      to: "placeholder convention note"
      via: "blockquote at line 1-3 before any code examples"
      pattern: "<your-coolify-domain>"
---

<objective>
Genericize `references/api-reference.md` by replacing all maintainer-specific domain values (`coolify.cicd.streamlinity.com`, `streamlinity-staging.streamlinity.com`, `streamlinity` Doppler account name) with placeholders. Add a one-line note at the top of the file explaining the placeholder convention so readers know to substitute their own values.

Purpose: The file is currently linked from SKILL.md as the API reference any new user might read while integrating. Maintainer-specific examples confuse readers who don't recognize that `streamlinity` is the maintainer's Doppler workspace and not a required value.

Output: Updated `references/api-reference.md` with placeholders and a top-of-file substitution note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md
@references/api-reference.md
</context>

<interfaces>
<!-- All maintainer-specific strings in references/api-reference.md as of phase start. -->
<!-- Output of: grep -n "streamlinity\|coolify.cicd" references/api-reference.md -->

```
  5: Base URL: `https://coolify.cicd.streamlinity.com/api/v1` (after SSL enabled + allowed_ips cleared)
 70:     "domains": "https://skillmap-staging.streamlinity.com",
 89:     "fqdn": "https://skillmap-staging.streamlinity.com",
190: doppler --account streamlinity configs tokens create "coolify-staging" \
204: doppler --account streamlinity secrets download \
218: doppler --account streamlinity secrets get \
```

Replacements (per D-09):
- `coolify.cicd.streamlinity.com` → `<your-coolify-domain>` (so the base URL becomes `https://<your-coolify-domain>/api/v1`)
- `streamlinity.com` (within `skillmap-staging.streamlinity.com`) → `<your-app-domain>` (keep `skillmap-staging.` prefix as illustrative example; substitute only the maintainer's apex domain)
- `--account streamlinity` → `--account <your-doppler-account>`

Note: the bare word `streamlinity` only appears in the contexts listed above. There are no other references to the maintainer that need replacement.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add top-of-file placeholder note and replace all maintainer-specific values</name>
  <files>references/api-reference.md</files>
  <read_first>
    - references/api-reference.md (full file — must read current state to verify replacement count matches the 6 occurrences listed in the interfaces block)
    - .planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md (D-09 — exact placeholder values and note wording)
  </read_first>
  <action>
    Apply two coordinated edits.

    EDIT 1 — Insert a placeholder convention note at the very top of the file. The current file starts with:
    ```
    # Coolify + Doppler API Reference

    Source: `.planning/phases/07-coolify-doppler-deploy-infrastructure/07-RESEARCH.md`

    Base URL: `https://coolify.cicd.streamlinity.com/api/v1` (after SSL enabled + allowed_ips cleared)
    ```

    Insert a blockquote note immediately after the `# Coolify + Doppler API Reference` heading and before the `Source:` line:

    ```
    # Coolify + Doppler API Reference

    > Replace `<your-coolify-domain>` with your Coolify instance root URL (e.g. `coolify.example.com`) and `<your-doppler-account>` with your Doppler account slug throughout this document. The examples use placeholder values; you must substitute your own.

    Source: `.planning/phases/07-coolify-doppler-deploy-infrastructure/07-RESEARCH.md`

    Base URL: `https://<your-coolify-domain>/api/v1` (after SSL enabled + allowed_ips cleared)
    ```

    EDIT 2 — Replace every remaining maintainer-specific value in the file:

    | Old | New |
    |-----|-----|
    | `coolify.cicd.streamlinity.com` | `<your-coolify-domain>` |
    | `skillmap-staging.streamlinity.com` | `skillmap-staging.<your-app-domain>` |
    | `--account streamlinity` | `--account <your-doppler-account>` |

    Use exact-string replacement — do NOT touch the surrounding code (the example `curl` commands, JSON payloads, and `doppler` invocations should keep their structure; only the highlighted tokens change). The base URL line in the file header (line 5 before this edit) is already covered by EDIT 1.

    After both edits, the following invariants must hold:
    - The word `streamlinity` appears ZERO times in the file.
    - The string `coolify.cicd` appears ZERO times in the file.
    - The placeholders `<your-coolify-domain>`, `<your-doppler-account>`, and `<your-app-domain>` each appear at least once.
    - The top-of-file note appears before the first `## ` section.

    Do NOT modify any other content: keep all `curl` invocations, all JSON payloads, all `Response:` comments, all section headings, all numbered list ordering exactly as they are.
  </action>
  <verify>
    <automated>cd /home/cnut/development/claude-skills-deploy && grep -c 'streamlinity' references/api-reference.md && grep -c 'coolify.cicd' references/api-reference.md && grep -q '<your-coolify-domain>' references/api-reference.md && grep -q '<your-doppler-account>' references/api-reference.md && grep -q '<your-app-domain>' references/api-reference.md && grep -q '> Replace `<your-coolify-domain>`' references/api-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'streamlinity' references/api-reference.md` returns 0 (zero occurrences — the word does not appear anywhere)
    - `grep -c 'coolify.cicd' references/api-reference.md` returns 0
    - `grep -q '<your-coolify-domain>' references/api-reference.md` (placeholder present)
    - `grep -q '<your-doppler-account>' references/api-reference.md` (placeholder present)
    - `grep -q '<your-app-domain>' references/api-reference.md` (placeholder present in the per-app FQDN examples)
    - `grep -q '> Replace `<your-coolify-domain>`' references/api-reference.md` (top-of-file note present as a blockquote)
    - The placeholder note appears BEFORE the first `## ` (H2) section (verifiable by line number: line of the note < line of first `^## ` match)
    - `grep -c 'Base URL: `https://<your-coolify-domain>/api/v1`' references/api-reference.md` returns 1 (the original base-URL line was rewritten, not duplicated)
    - File still ends with `## Notes` section content intact (the rewrite did not truncate the file): `grep -q '## Notes' references/api-reference.md`
  </acceptance_criteria>
  <done>references/api-reference.md is domain-neutral. A reader sees the substitution convention before the first example, and every `curl` and `doppler` invocation uses placeholders instead of maintainer-specific values.</done>
</task>

</tasks>

<verification>
- `grep -c 'streamlinity' references/api-reference.md` → 0
- `grep -c 'coolify.cicd' references/api-reference.md` → 0
- File renders as valid markdown (no broken code fences — verify by counting backtick triples: `grep -c '^```' references/api-reference.md` returns an even number)
</verification>

<success_criteria>
- The API reference is usable by any reader without knowing what `streamlinity` is or why it appears
- The placeholder convention is documented inline at the top of the file so readers don't have to infer it from context
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-new-user-onboarding/02.1-04-SUMMARY.md`
</output>
