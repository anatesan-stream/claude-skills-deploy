---
phase: 02.1-new-user-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - ONBOARD-06

must_haves:
  truths:
    - "README.md has a new ## Quick start section positioned ABOVE the existing ## Prerequisites section"
    - "Quick start lists exactly 5 commands in this order: clone the skill, /setup-coolify init, init.sh bootstrap, /setup-coolify validate, /setup-coolify"
    - "Each Quick start command has a one-line description of what it does and what it produces"
    - "Quick start links to docs/setup-guide.md (full VPS setup) and docs/fork-guide.md (adding a second domain)"
  artifacts:
    - path: "README.md"
      provides: "5-command quick-start visible to new users before the prerequisites wall of text"
      contains: "## Quick start"
  key_links:
    - from: "README.md ## Quick start"
      to: "docs/setup-guide.md"
      via: "markdown link"
      pattern: "docs/setup-guide.md"
    - from: "README.md ## Quick start"
      to: "docs/fork-guide.md"
      via: "markdown link"
      pattern: "docs/fork-guide.md"
---

<objective>
Add a "Quick start" section to `README.md` positioned above the existing "Prerequisites" section. New users currently land on a wall of prerequisites before seeing how to actually use the skill — moving a 5-step happy path above the prerequisites lets them grasp the workflow in 30 seconds before deciding whether to invest in the setup steps.

Purpose: Onboarding friction. CONCERNS.md flagged that the README's structure (Prerequisites → Install → Bootstrap → Run → Full setup) does not give new users a top-of-document "what does it look like to use this" overview.

Output: Updated `README.md` with a new `## Quick start` section inserted between `## What you get` and `## Prerequisites`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md
@README.md
@docs/setup-guide.md
@docs/fork-guide.md
</context>

<interfaces>
<!-- Current README.md top-level section structure (extracted via grep "^## " README.md). -->
<!-- The new Quick start section goes between "## What you get" and "## Prerequisites". -->

```
## What you get           (line 11)   ← INSERT new "## Quick start" AFTER this section
## Prerequisites          (line 20)   ← new section goes BEFORE this
## Install                (line 46)
## First-time configuration (per Coolify server)  (line 63)
## Bootstrap a new repo   (line 88)
## Subcommands            (line 114)
## Forking for a new domain (line 124)
## Schema reference       (line 132)
## Troubleshooting        (line 138)
## How it works           (line 152)
## E2E integration test   (line 167)
## See also               (line 196)
```

The "What you get" section ends with the `---` divider on line 18 (approximately). The new section must be inserted between that `---` and the `## Prerequisites` heading.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert ## Quick start section above ## Prerequisites in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — must read current structure to find exact insertion point between ## What you get and ## Prerequisites)
    - .planning/phases/02.1-new-user-onboarding/02.1-CONTEXT.md (D-08 — required 5 commands and link requirements)
    - docs/setup-guide.md (skim only — to confirm it exists and is the right link target for "full VPS setup prerequisites")
    - docs/fork-guide.md (skim only — to confirm it exists and is the right link target for "adding a second domain")
  </read_first>
  <action>
    Locate the divider `---` that follows the `## What you get` section (currently around line 18, just before `## Prerequisites` on line 20). Insert a new `## Quick start` section between them. The inserted block must contain:

    1. An H2 heading: `## Quick start`
    2. A one-line lead-in explaining this is the happy path; full prerequisites are below.
    3. A numbered list of EXACTLY 5 commands in this order, each with a one-line description placed on the line below (markdown ordered list with sub-bullets is acceptable; a numbered list with description after a dash is also acceptable — exact formatting is at Claude's discretion).
    4. A closing paragraph linking to `docs/setup-guide.md` (for full VPS setup) and `docs/fork-guide.md` (for adding a second domain).
    5. A trailing `---` divider.

    The 5 commands MUST be exactly these, in this order:

    | # | Command | One-line description |
    |---|---------|----------------------|
    | 1 | `git clone https://github.com/anatesan-stream/claude-skills-deploy.git ~/.claude/skills/setup-coolify` | Installs the skill into your personal Claude Code skills directory. After this, `/setup-coolify` is available in any Claude Code session. |
    | 2 | `/setup-coolify init` | Interactive setup of `~/.claude/coolify.json` for your Coolify server (URL, API key, Doppler account, SSH host). One-time per machine + server pair. |
    | 3 | `bash ~/.claude/skills/setup-coolify/init/init.sh` | Bootstraps the current repo. Writes `./coolify.yaml` and `./.github/workflows/deploy.yml`. |
    | 4 | `/setup-coolify validate` | Dry-run check: verifies all `env_vars` in `coolify.yaml` exist in Doppler and the Coolify API is reachable. No mutations. |
    | 5 | `/setup-coolify` | Provisions Coolify staging + production apps, wires Doppler service tokens, mounts the Doppler-cache volume. Idempotent. After this, push to `main` triggers the first deploy via the generated workflow. |

    Use this exact block (the executor may adjust whitespace to match the surrounding file's style, but the content and ordering are fixed):

    ```markdown
    ## Quick start

    The happy path in 5 commands. Full prerequisites (Doppler CLI, Python, an actual Coolify server) are documented below — see [Prerequisites](#prerequisites) and [docs/setup-guide.md](./docs/setup-guide.md) before running any of these against a real server.

    1. **Install the skill**
       ```bash
       git clone https://github.com/anatesan-stream/claude-skills-deploy.git ~/.claude/skills/setup-coolify
       ```
       Drops the skill into your personal Claude Code skills directory. After this, `/setup-coolify` is available in any Claude Code session.

    2. **Configure your Coolify server (one-time per machine + server pair)**
       ```
       /setup-coolify init
       ```
       Interactive prompts populate `~/.claude/coolify.json` with the Coolify URL, API key, Doppler account, and SSH host for one server alias.

    3. **Bootstrap a repo**
       ```bash
       bash ~/.claude/skills/setup-coolify/init/init.sh
       ```
       Run from the target repo's root. Writes `./coolify.yaml` and `./.github/workflows/deploy.yml`.

    4. **Dry-run check**
       ```
       /setup-coolify validate
       ```
       Verifies every `env_vars` key in `coolify.yaml` exists in Doppler and the Coolify API is reachable. No mutations.

    5. **Provision**
       ```
       /setup-coolify
       ```
       Creates Coolify staging + production apps, wires Doppler service tokens, mounts the Doppler-cache Docker volume. Idempotent. After this, push to `main` triggers the first deploy via the generated `.github/workflows/deploy.yml`.

    Need more? See [docs/setup-guide.md](./docs/setup-guide.md) for the full VPS + Coolify + Doppler stand-up walkthrough, or [docs/fork-guide.md](./docs/fork-guide.md) for adding a second domain to an existing setup.

    ---
    ```

    Insertion point: place this block AFTER the `---` divider that closes `## What you get` and BEFORE the `## Prerequisites` heading. Do NOT modify any other section of README.md.
  </action>
  <verify>
    <automated>cd /home/cnut/development/claude-skills-deploy && grep -q '^## Quick start' README.md && python3 -c "import re; t=open('README.md').read(); qs=t.find('## Quick start'); pq=t.find('## Prerequisites'); wy=t.find('## What you get'); assert wy < qs < pq, f'order wrong: wy={wy} qs={qs} pq={pq}'; print('ok')" && grep -q 'git clone https://github.com/anatesan-stream/claude-skills-deploy.git ~/.claude/skills/setup-coolify' README.md && grep -q '/setup-coolify init' README.md && grep -q 'bash ~/.claude/skills/setup-coolify/init/init.sh' README.md && grep -q '/setup-coolify validate' README.md && grep -q './docs/setup-guide.md' README.md && grep -q './docs/fork-guide.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Quick start' README.md` (section exists at H2 level)
    - Position check: the `## Quick start` heading appears AFTER `## What you get` and BEFORE `## Prerequisites` in the file (verified by python ordering check in the automated command above)
    - All 5 required commands appear verbatim:
      - `grep -q 'git clone https://github.com/anatesan-stream/claude-skills-deploy.git ~/.claude/skills/setup-coolify' README.md`
      - `grep -q '/setup-coolify init' README.md` (appears in Quick start; may also appear elsewhere — that's fine)
      - `grep -q 'bash ~/.claude/skills/setup-coolify/init/init.sh' README.md`
      - `grep -q '/setup-coolify validate' README.md`
      - `grep -q '^/setup-coolify$\|`/setup-coolify`' README.md` (standalone command appears in Quick start; if grep returns the line inside Quick start, that's the match)
    - `grep -q './docs/setup-guide.md' README.md` (full-setup link present)
    - `grep -q './docs/fork-guide.md' README.md` (second-domain link present)
    - `test -f docs/setup-guide.md && test -f docs/fork-guide.md` (link targets exist)
    - The five commands appear in the order listed in the action table (executor confirms by reading the inserted block top-to-bottom)
    - No other section of README.md is modified (verifiable via `git diff README.md` review)
  </acceptance_criteria>
  <done>README.md opens with a 5-step happy path that fits on one screen, links out to deeper docs, and is positioned where a new visitor sees it before the prerequisites wall.</done>
</task>

</tasks>

<verification>
- `python3 -c "import re; t=open('README.md').read(); assert t.find('## What you get') < t.find('## Quick start') < t.find('## Prerequisites')"` exits 0
- Both linked docs (`docs/setup-guide.md`, `docs/fork-guide.md`) exist on disk
- README.md still renders (no broken markdown — table rows preserved, code fences closed)
</verification>

<success_criteria>
- A first-time visitor to the GitHub repo sees the 5-command happy path above the fold (within the first screen of scrolling) instead of a prerequisites wall
- The Quick start uses only commands that exist and are described accurately
- Deep-dive links direct users to the right place for VPS setup vs. multi-domain
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-new-user-onboarding/02.1-03-SUMMARY.md`
</output>
