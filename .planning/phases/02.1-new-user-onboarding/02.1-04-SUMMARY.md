---
phase: 02.1-new-user-onboarding
plan: "04"
subsystem: docs
tags: [api-reference, coolify, doppler, placeholders, onboarding]

# Dependency graph
requires: []
provides:
  - Domain-neutral Coolify + Doppler API reference with placeholder substitution convention
  - ROADMAP SC #2 satisfied for references/api-reference.md (cicd substring removed)
affects:
  - 02.1-new-user-onboarding (ROADMAP SC #2 verification)
  - any future reader or fork of the skill consulting the API reference

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Placeholder convention: <your-coolify-domain>, <your-doppler-account>, <your-app-domain>, <your-ssh-host> as generic substitution tokens in docs"

key-files:
  created: []
  modified:
    - references/api-reference.md

key-decisions:
  - "Use <your-coolify-domain> (not coolify.example.com) as placeholder — angle-bracket form is self-evidently a placeholder, unlike an example.com URL that could be mistaken for a real value"
  - "Place substitution note as a blockquote immediately after the H1 heading, before the Source line — first thing a reader sees"
  - "Keep skillmap-staging. prefix in app domain examples — illustrative and domain-neutral; only the apex domain (streamlinity.com) is maintainer-specific and gets replaced"

patterns-established:
  - "Blockquote convention at top of reference docs for placeholder substitution instructions"

requirements-completed:
  - ONBOARD-07

# Metrics
duration: 1min
completed: 2026-05-22
---

# Phase 02.1 Plan 04: Genericize API Reference Summary

**Domain-neutral `references/api-reference.md` with top-of-file placeholder note and all maintainer-specific values replaced with `<your-coolify-domain>`, `<your-doppler-account>`, `<your-app-domain>`, and `<your-ssh-host>`**

## Performance

- **Duration:** 1 min
- **Started:** 2026-05-22T19:26:38Z
- **Completed:** 2026-05-22T19:27:30Z
- **Tasks:** 1 of 1
- **Files modified:** 1

## Accomplishments

- Inserted blockquote substitution convention note after the H1 heading (line 3) — first thing any reader sees before any example
- Replaced `coolify.cicd.streamlinity.com` with `<your-coolify-domain>` in the base URL header line
- Replaced `skillmap-staging.streamlinity.com` (2 occurrences in JSON payloads) with `skillmap-staging.<your-app-domain>`
- Replaced `--account streamlinity` (3 occurrences in doppler CLI invocations) with `--account <your-doppler-account>`
- Replaced `v_cicd_stream` (Notes section ssh example) with `<your-ssh-host>` — satisfies ROADMAP SC #2 (`cicd` substring removed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add top-of-file placeholder note and replace all maintainer-specific values** - `8da7bb9` (docs)

## Files Created/Modified

- `references/api-reference.md` - Added placeholder substitution blockquote note; replaced all 7 maintainer-specific occurrences with generic placeholders

## Decisions Made

- Used angle-bracket placeholders (`<your-coolify-domain>`) rather than `coolify.example.com` style — angle brackets are self-evidently tokens to substitute, not real hostnames
- Kept `skillmap-staging.` prefix intact in app-domain examples — the prefix is illustrative (shows how subdomains are structured); only the maintainer's apex domain is replaced
- Placed blockquote note immediately after H1 (line 3), before the `Source:` metadata line — ensures it appears before any code examples

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None — the file is purely documentation with placeholder values; no data sources or runtime stubs.

## Next Phase Readiness

- `references/api-reference.md` is fully domain-neutral; any new user reading it will see the substitution instructions before any example
- ROADMAP SC #2 (`cicd` substring) is now satisfied for this file
- Verification: `! grep -q 'streamlinity\|cicd' references/api-reference.md` passes

---
*Phase: 02.1-new-user-onboarding*
*Completed: 2026-05-22*
